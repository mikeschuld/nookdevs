<?xml version="1.0" encoding="utf-8"?>

<resources>
    <!-- XXX: For reasons unknown, this definition won't work on the nook (though it works in the
              emulator).  Resorted to using a predefined theme for the time being. -->
    <style name="dialog" parent="android:Theme.Panel">
        <item name="android:animation">@anim/none</item>
        <item name="android:activityCloseEnterAnimation">@anim/none</item>
        <item name="android:activityCloseExitAnimation">@anim/none</item>
        <item name="android:activityOpenEnterAnimation">@anim/none</item>
        <item name="android:activityOpenExitAnimation">@anim/none</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:panelBackground">#c8c8c8</item>
        <item name="android:textColor">#333333</item>
        <item name="android:title">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
